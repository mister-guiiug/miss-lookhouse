// Setup Vitest partagé : jest-dom + stub matchMedia + mocks virtual:pwa-register
// + polyfill localStorage in-memory (fourni par dev-pwa-config).
import '@mister-guiiug/dev-pwa-config/vitest-setup';
