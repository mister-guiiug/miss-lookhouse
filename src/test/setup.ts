// Setup Vitest partagé : jest-dom + stub matchMedia + mocks virtual:pwa-register
// + polyfill localStorage in-memory (fourni par dev-wpa-config).
import '@mister-guiiug/dev-wpa-config/vitest-setup';
