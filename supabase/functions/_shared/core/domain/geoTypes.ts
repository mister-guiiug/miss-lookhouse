// @generated par scripts/build-edge-core.mjs — NE PAS ÉDITER.
// Source : src/domain/geoTypes.ts · Régénérer : npm run build:edge-core

/** Type géographique minimal partagé (évite les cycles d'import). */
export interface GeoPoint {
  lat: number;
  lng: number;
}
